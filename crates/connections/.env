# Local dev DB URL for sqlx compile-time query checking (connections database).
# Non-secret localhost credentials, identical to docker-compose.yml. Builds with
# committed .sqlx/ and SQLX_OFFLINE=true do not need this.
DATABASE_URL=postgres://iam_connections:iam_connections@localhost:5432/iam_connections
